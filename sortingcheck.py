# checking an array if it is sorted.

# sort check using built-in function.
def sorted_list(arr):
    reversed = sorted(arr)
    if arr == reversed:
        return  "sorted."
    else:
        return "not sorted."
array = [1,2,3,4]
# print(sorted_list(array)

